namespace DeckBattle
{
    // Typed, presentation-only signals emitted by animation clips through UnitAnimationEventRelay.
    public enum UnitAnimationVfxSignal
    {
        AttackContact = 0,
        ProjectileRelease = 1,
        SpecialContact = 2
    }
}
